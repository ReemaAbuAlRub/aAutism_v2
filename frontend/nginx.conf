# server {
#     listen 80;
#     server_name localhost;
#     root /usr/share/nginx/html;
#     index index.html;

#   location /api/ {
#     proxy_pass http://backend;   # << no trailing slash, no :8000
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_read_timeout 60s;
#   }

# #     # Proxy API requests to backend
# # location /api/ {
# #   proxy_pass http://backend;   # no trailing slash -> sends /api/... to backend
# #   proxy_http_version 1.1;
# #   proxy_set_header Host $host;
# #   proxy_set_header X-Real-IP $remote_addr;
# #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
# #   proxy_set_header X-Forwarded-Proto $scheme;
# # }

#     # Optional: serve static assets with caching
#     # location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
#     #     expires 1y;
#     #     add_header Cache-Control "public, immutable";
#     # }
#     location / { try_files $uri /index.html; 
#     }
# }

# frontend/nginx.azure.conf
# server {
#   listen 80;
#   server_name _;
#   root /usr/share/nginx/html;
#   index index.html;

#   # For Azure Container Apps, you need to use the backend's external URL
#   # Replace with your actual backend URL from Azure
#   location /api/ {
#     proxy_pass https://backend.ashymeadow-e605a82c.uaenorth.azurecontainerapps.io/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_read_timeout 60s;
    
#     # Handle CORS preflight requests
#     if ($request_method = 'OPTIONS') {
#       add_header 'Access-Control-Allow-Origin' '*';
#       add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE';
#       add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
#       add_header 'Access-Control-Max-Age' 1728000;
#       add_header 'Content-Type' 'text/plain; charset=utf-8';
#       add_header 'Content-Length' 0;
#       return 204;
#     }
#   }

#   # Optional: expose FastAPI docs (remove in production)
#   location = /docs { 
#     proxy_pass https://backend.ashymeadow-e605a82c.uaenorth.azurecontainerapps.io/docs;
#   }
#   location = /openapi.json { 
#     proxy_pass https://backend.ashymeadow-e605a82c.uaenorth.azurecontainerapps.io/openapi.json;
#   }

#   # SPA fallback
#   location / {
#      try_files $uri /index.html;
#   }
# }


server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass https://backend.ashymeadow-e605a82c.uaenorth.azurecontainerapps.io;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
  }

  location / {
    try_files $uri /index.html;
  }
}